extends layout 

block content 
      .w3-card-4
         header.w3-container.w3-teal
               h1 Aluno #{a.id}
         .w3-container
               ul.w3-ul.w3-card-4
                    li
                        b Nome= #{a.nome}
                    li
                        b Número= #{a.id}
                    li
                        b GitLink= #{a.github}

               .w3-container.w3-margin-8
         ul.w3-ul
         each val in [1,2,3,4,5,6,7,8]
            var tpc = "tpc#{val}"
            if tpc in a
               li= tpc  
         footer.w3-container.w3-teal
               h5 Gerado por galuno::RPCW2022 em #{d} 
               a(href="/") Voltar

